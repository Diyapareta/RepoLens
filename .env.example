# Optional: raises GitHub's unauthenticated API rate limit for local development.
# Never put this value in the frontend or commit a real token.
GITHUB_TOKEN=
PORT=4000

# Keep these server-side only. Never place either value in apps/web.
GEMINI_API_KEY=
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
